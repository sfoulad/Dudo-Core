{
  "name": "dudo-core",
  "version": "0.0.0",
  "private": true,
  "description": "Dudo-Core — deployment tooling for the Cloudflare Worker. See wrangler.jsonc.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "//typecheck": "Both trees. `packages/testing` was checked by NOTHING until 2026-09-05, which concealed a fixture that had silently stopped composing what production composes while 400+ assertions kept passing — the suites reported on a shape the product no longer had. The gate landed red with 14 diagnostics and was deliberately kept OUT of this script until it was green, because a red gate folded into a green one makes the green one worthless. It is green as of 2026-09-05 and is now part of the gate. Three of the original 14 were exactly the drift it was added to catch.",
    "typecheck": "npm run typecheck:core && npm run typecheck:tests && npm run check:contracts",
    "typecheck:core": "tsc --noEmit",
    "typecheck:tests": "tsc -p packages/testing/tsconfig.json --noEmit",
    "//check:contracts": "Unsatisfiable request shapes: a `required` field absent from `properties` under `additionalProperties: false` can be satisfied by NO request. Nothing here executes JSON Schema, so this is invisible to typecheck, to every suite, and to review — `required` and `properties` are read separately and each looks correct alone. It found FOUR real instances on its first run (2026-09-05), one of which `core-agent` was mid-build against. It was kept OUT of `typecheck` while RED on `login-v1`'s `loginCompleteRequest` — which required `email` and `derived_key` and declared NEITHER, its `properties` block absent entirely — because folding a red gate into a green one makes the green one worthless, the same reason `typecheck:tests` was kept out that morning. `login-v1` was fixed the same day and it is now part of the gate. It is green at 17 files / 84 closed objects; if it ever reports FEWER objects than that without the contract set shrinking, the walk has stopped finding things and the green means nothing.",
    "check:contracts": "node scripts/check-contract-shapes.mjs",
    "//migrations": "LOCAL variants act on the local simulated D1 and are safe to run freely. REMOTE variants act on the deployed database and are a production-class action requiring explicit user approval each time (.claude/rules/security.md §7). They are separate scripts, and named, so neither can be run by accident.",
    "d1:migrate:tenant:local": "wrangler d1 migrations apply dudo-tenant --local",
    "d1:migrate:control:local": "wrangler d1 migrations apply dudo-control-plane --local",
    "d1:migrate:tenant:remote": "wrangler d1 migrations apply dudo-tenant --remote",
    "d1:migrate:control:remote": "wrangler d1 migrations apply dudo-control-plane --remote",
    "//deploy": "There is no `deploy` script, deliberately. Deployment is gated on C5 and on explicit user approval; a one-word npm script is exactly the wrong affordance for an action that needs a decision.",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.129.0"
  }
}
