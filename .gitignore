# === OS / editor ===
.DS_Store
Thumbs.db

# === Local environment ===
.env
.env.*
!.env.example

# Wrangler's local secrets file. NOT covered by the .env patterns above, and it is
# where CURSOR_SIGNING_KEY, SESSION_HMAC_KEY and IDENTITY_LOOKUP_KEY live during
# `wrangler dev`. Both repositories are public (.claude/rules/architecture.md §8);
# a credential committed here is compromised on landing and survives deletion.
.dev.vars
.dev.vars.*
!.dev.vars.example

# Wrangler's local state, including the local D1 databases created by
# `wrangler d1 migrations apply --local`. These contain seeded principals.
.wrangler/

# === Internal AI-agent working files (deliberately NOT published) ===
# The agent definitions, binding rules, and Claude Code settings stay local.
# Public engineering standards live in docs/ and CONTRIBUTING.md instead.
.claude/
CLAUDE.md

# === Secrets and credential material ===
# This repository is PUBLIC. Nothing below may ever be committed.
# A secret that lands in public git history is compromised and must be rotated —
# deleting the file does not undo it.
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.der
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
credentials.json
secrets.yml
secrets.yaml
*.secret
*.secrets

# === Apple signing material (relevant to Dudo-Apple; kept here as a shared guard) ===
*.mobileprovision
*.provisionprofile
*.cer
*.certSigningRequest
*.p8
ExportOptions.plist
AuthKey_*.p8

# === Private planning documents ===
# The master build-plan PDF must never be committed or published.
# PDFs are excluded by default; to publish a specific one deliberately, add an
# explicit negation (e.g. "!docs/public-diagram.pdf") below this line.
*.pdf

# === Build output (populated once a stack is selected) ===
node_modules/
dist/
build/
.next/
.turbo/
coverage/
*.log

# === Xcode / macOS build artefacts ===
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
.build/
