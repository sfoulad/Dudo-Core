# packages/ — Shared Packages

Shared code that crosses module boundaries.

| Package | Owner | Purpose |
|---|---|---|
| `contracts/` | `core-agent` | Shared cross-module contracts — the only sanctioned way modules talk |
| `plugin-sdk/` | `plugin-agent` | The SDK plugins are built against: manifests, lifecycle, permissions |

Boundaries: `docs/architecture/boundaries.md` · Contributing: `CONTRIBUTING.md`

*Empty placeholder. No application code exists yet and no technology stack has been
selected.*
