{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:dudo:fixture:same-name-target:1",
  "title": "Same-name cross-schema $ref — the TARGET half",
  "$defs": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z0-9_-]{8,64}$" },
    "widgetName": { "type": "string", "minLength": 1 },
    "narrowedId": { "type": "string", "pattern": "^[A-Za-z0-9_-]{8,64}$" }
  }
}
