{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:dudo:schema:credential-reset:1",
  "title": "Credential reset — wire shapes, version 1",
  "description": "The machine-readable half of credential-reset-v1.contract.yaml. BOTH ARE NORMATIVE TOGETHER. Nothing in this repository executes JSON Schema, so this file is normative and hand-checked, never machine-validated.",

  "$defs": {

    "resetCredentialInput": {
      "type": "object",
      "additionalProperties": false,
      "required": ["principal_id", "identifier", "derived_value", "confirmation_id", "reauth_derived_value"],
      "description": "ONE FIELD, AND THE ABSENCES ARE THE DESIGN. NO email, identifier or search field — a route accepting an address would be a platform-wide account-existence oracle, handing back through an API exactly what credential-store.ts spends a keyed HMAC to withhold. NO password or derived_value from the operator's choosing — 0017's basis is that entropy protects these accounts, so a human-chosen value expires that decision. NO organization_id — the reset is a control-plane operation and naming a tenant would not scope it. NO 'notify' or 'reason' field; the reason belongs in the audit record, which Core writes.",
      "properties": {
        "principal_id": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]{8,64}$",
          "description": "The principal whose credential is replaced. THIS IS HOW THE TARGET IS FOUND — `identifier` below is verified against it and is never an index into anything. NOTE THE PRACTICAL GAP RECORDED AS CR-3: no route in any Dudo contract currently produces a principal_id, because nothing lists an Organization's members, so this route is unusable until one exists."
        },
        "identifier": {
          "type": "string",
          "minLength": 3,
          "maxLength": 254,
          "pattern": "^[!-?A-~]*@[!-?A-~]*$",
          "description": "THE TARGET'S LOGIN IDENTIFIER, SUPPLIED BY THE OPERATOR. ADDED 2026-09-05 AND FORCED BY ARITHMETIC — see CR-5. It is NOT a lookup key: Core computes HMAC-SHA-256(IDENTITY_LOOKUP_KEY, normalize(identifier)) and requires it to equal the stored identifier_hash for the named principal, and a mismatch returns the same argument-free 404 as an unknown principal. IT IS REQUIRED BECAUSE THE KDF SALT IS THE NORMALISED IDENTIFIER AND NOBODY ELSE HAS IT: the console has never seen it, the server stores only its keyed hash and so genuinely cannot recover it, and the target is not present to derive. Without it, a reset would write a verifier the target could never reproduce at login and would permanently brick the account it was meant to rescue. THE ORIGINAL RULING FORBADE ANY IDENTIFIER FIELD, and what that objection was really about — a route that finds a principal BY email, which is a platform-wide account-existence oracle — remains impossible here."
        },
        "derived_value": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]{43}$",
          "description": "32 bytes, base64url. PRESENT ONLY IF ON-5/CR-5 IS DECIDED AS OPTION B — the admin console generates the password, derives PBKDF2-SHA256(NFC(password), salt = the target's normalized identifier, 600000, 32 bytes), and sends the derived value; the server hashes it at 10,000 as usual. THE PASSWORD ITSELF NEVER REACHES THE SERVER UNDER ANY OPTION. Declared optional pending that decision rather than the shape being written twice. NOTE THE COMPLICATION OPTION B CARRIES HERE AND NOT IN ONBOARDING: the KDF salt is the target's NORMALISED IDENTIFIER, which the console does not know and cannot be told — the control plane stores only a keyed hash of it. Resolving this is part of CR-5 and it may force option A for this route alone."
        }
      }
    },

    "resetWarning": {
      "type": "string",
      "enum": ["sessions_not_revoked", "tenant_audit_incomplete"],
      "description": "A CLOSED SET OF STABLE TOKENS, never free text, so a console can render each specifically and qa-agent can assert them. Both report a BOUNDED, DELIBERATE incompleteness in a security operation — the 50-session cap and the 10-Organization audit cap — and both exist because the alternative to a cap is one reset consuming the platform's entire daily write budget."
    },

    "resetCredentialOutput": {
      "type": "object",
      "additionalProperties": false,
      "required": ["principal_id", "new_password", "sessions_revoked", "warnings"],
      "description": "Cache-Control: no-store. THIS AND THE ONBOARDING RESPONSE ARE THE ONLY TWO BODIES IN ANY DUDO CONTRACT THAT CARRY A CREDENTIAL.",
      "properties": {
        "principal_id": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_-]{8,64}$",
          "description": "Echoed so a console can confirm which account it just took over. It carries nothing the caller did not send."
        },
        "new_password": {
          "type": "string",
          "minLength": 20,
          "maxLength": 64,
          "description": "THE GENERATED PASSWORD, IN PLAINTEXT, SHOWN EXACTLY ONCE AND NEVER AGAIN BY ANY ROUTE. At least 128 bits of entropy from a cryptographic source, in an alphabet with no visually confusable characters, because a human reads it aloud or retypes it from a screen. IT IS NOT STORED: principal_credential holds a hash of a hash of it and there is no reversible form. An operator who closes the tab must reset again. THE CONSOLE MUST SAY THAT AT THE MOMENT IT DISPLAYS THIS VALUE. It appears in no log, no metric, no trace attribute, no error message and NO AUDIT RECORD — the audit record says a reset happened, never what the value was."
        },
        "sessions_revoked": {
          "type": "integer",
          "minimum": 0,
          "maximum": 50,
          "description": "How many live sessions were ended. ZERO IS AN ORDINARY AND COMMON ANSWER — the target simply was not logged in — and a console must not present it as a failure. A NON-ZERO VALUE IS THE OPERATIONALLY INTERESTING ONE: it means someone was actively using that account at the moment it was taken over, which is precisely what an operator resetting a suspected-compromised account needs to see."
        },
        "warnings": {
          "type": "array",
          "maxItems": 2,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/resetWarning" },
          "description": "EMPTY ON A COMPLETE OPERATION. A non-empty array means part of a SECURITY operation did not complete — sessions survive, or an Organization's copy of the trail is missing — and a console that renders it as an ordinary success has hidden the one thing an operator needed to act on."
        }
      }
    }
  }
}
